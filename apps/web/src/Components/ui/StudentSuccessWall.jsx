/**
 * Optional social-proof strip on the Academy page (between pricing and Jobs Hub).
 * Renders nothing until real content is wired; keeps the page module graph valid for Vite.
 */
export function StudentSuccessWall() {
  return null;
}
